/** Row already synced from Engage — stored in DB, do not push again. */
export const DATA_SOURCE_ENGAGE = "engage";

/** Row created/updated in Task Master — push to Engage on webhook. */
export const DATA_SOURCE_TASK_MASTER = "task_master";
