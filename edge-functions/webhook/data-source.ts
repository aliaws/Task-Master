/**
 * Label only — who created the row (not used to skip webhook push).
 *
 * engage       — imported from Engage/GHL (sync-ghl)
 * task_master  — created or edited in Task Master app
 */

export const DATA_SOURCE_ENGAGE = "engage";
export const DATA_SOURCE_TASK_MASTER = "task_master";
